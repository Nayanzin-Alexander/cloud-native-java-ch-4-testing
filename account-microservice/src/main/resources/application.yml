spring:
  jpa:
    show_sql: false
    database: H2
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: embedded
    platform: H2
  data:
    rest:
      base-path: /api
user-service.host: ${USER_SERVICE_HOST:localhost:48080}
